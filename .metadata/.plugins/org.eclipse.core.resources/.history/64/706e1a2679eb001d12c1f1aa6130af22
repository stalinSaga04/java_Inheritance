package usingWhileLoops;

public class BinaryToDecimal {
public static void main(String[] args) {
	BinaryToDecimal b2d = new BinaryToDecimal();
	b2d.find_decimal(101);
	System.out.println(b2d.find_decimal(101));
	b2d.swap_number(10,100);
	b2d.find_prime();
	
}	

private int find_decimal(int num) {
	int decimal = 0,rem = 0,pow=0;
	while(num>0) {
		rem = num%10;
		decimal = decimal+(rem *power_of(2, pow));
		num = num/10;
		pow+=1;
		
	}
	
	
	return decimal;
}
private int power_of(int base, int pow){
	int res = 1;
	while(pow>0) {
		res = res * base;
		pow = pow-1;
	}
	return pow;
}
private int swap_number(int n1, int n2) {
	
	return 0;
}
private int find_prime() {
	return 0;
}
}
